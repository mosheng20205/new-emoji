# new_emoji 组件封装进度

进度来源：计划内 Element 组件与服务式反馈组件入口，排除窗口创建命令。

## 总览

- 组件总数：`91`
- 已有创建/显示导出：`91`
- 已完成组件：`91`
- 未完成组件：`0`
- 完成度：`100.00%`

### Table 单元格双击编辑

- `Table` 新增普通文本单元格双击原地编辑能力：默认关闭，支持表级总开关、列级覆盖和单元格级覆盖；覆盖值统一为 `-1=继承、0=禁用、1=启用`。
- 新增 `TableCellEditCallback` 与 `EU_SetTableDoubleClickEdit`、`EU_SetTableColumnDoubleClickEdit`、`EU_SetTableCellDoubleClickEdit`、`EU_GetTableCellDoubleClickEditable`、`EU_GetTableDoubleClickEditState`、`EU_SetTableCellEditCallback`；回调 `action=1` 开始、`2` 提交、`3` 取消，文本按 UTF-8 字节数组 + 长度传递。
- 已同步 C++ 导出、`.def`、Python ctypes/helper、C# Native 生成器与 `EmojiTable` 对象式封装、易语言/Python/C# DLL 命令文档、Table 组件文档、API 索引、AI SDK 文档和火山模块生成源。
- 新增 `tests/python/test_table_double_click_edit_components.py`，覆盖开关优先级、真实双击编辑、Enter 提交、Esc 取消、失焦提交、UTF-8 回调和虚表禁用；已通过 x64/Win32 Release 构建、Table 回归、AI SDK 校验和火山模块 check。
- 本轮是 Table 已完成组件的交互能力补齐，不新增 Element 组件计数；当前组件总数：`91/91`，还剩 `0` 个未完成，完成度 `100.00%`。

### Window Frame 真实窗口圆角

- 完善 `EU_SetWindowRoundedCorners`：支持 DWM 系统抗锯齿圆角优先，Win10 回退为 per-pixel alpha 分层窗口，关闭后恢复矩形窗口。
- `EU_WINDOW_FRAME_ROUNDED` 与 `EU_SetWindowFrameFlags` 复用同一实现；窗口缩放、DPI 变化和跨屏缩放变化后会重新应用圆角。Win10 分层窗口回退模式会渲染 32bpp PBGRA 位图并通过 `UpdateLayeredWindow(ULW_ALPHA)` 提交，自绘标题栏会自动避开大圆角切角，避免标题和窗口按钮被裁掉；不再把 `SetWindowRgn` 作为最终圆角方案。
- 已同步 Python 测试、窗口框架文档、API 索引、三份 DLL 命令文档和 AI SDK 说明；本轮是窗口服务能力补齐，不新增 Element 组件计数。
- 当前组件总数：`91/91`，还剩 `0` 个未完成，完成度 `100.00%`。

### EditBox 多行光标命中修复

- 修复 `EditBox` 多行模式鼠标点击文字时光标定位不准确的问题：点击命中现在使用 DirectWrite `HitTestPoint` 按实际行列、滚动偏移和字体布局计算字符位置，不再沿用单行平均宽度估算。
- 补齐多行 `EditBox` 的键盘上下方向键导航：`VK_UP` / `VK_DOWN` 会按当前视觉列移动到上一行或下一行，并支持 Shift 扩展选区；左右方向键、滚轮、滚动条和单行行为保持兼容。
- 已重新生成 `Release|Win32` 与 `Release|x64` 的 `new_emoji.dll`，并同步 Win32 DLL 到 `T:\易语言源码\new_emoji易语言源码\new_emoji.dll` 供易语言示例直接运行。
- 本轮是 EditBox 已完成组件的行为修复，不新增 Element 组件计数；当前组件总数：`91/91`，还剩 `0` 个未完成，完成度 `100.00%`。

### Mentions 易语言全功能演示

- 新增 `T:\易语言源码\new_emoji易语言源码\MentionsAllFeaturesDemo.e.txt`，覆盖 `EU_CreateMentions`、`EU_SetMentionsValue`、`EU_SetMentionsSuggestions`、`EU_SetMentionsOpen`、`EU_SetMentionsSelected`、`EU_SetMentionsOptions`、`EU_SetMentionsFilter`、`EU_InsertMentionsSelected`、`EU_GetMentionsValue`、`EU_GetMentionsOpen`、`EU_GetMentionsSelected`、`EU_GetMentionsSuggestionCount` 和 `EU_GetMentionsOptions`。
- 演示源码遵守易语言交付规则：窗口标题、主标题、按钮、候选项、状态提示和核心展示项均使用 UTF-8 字节集 + 长度传入；源码中不直接写 emoji 字符。
- 首次窗口逻辑尺寸为 `1180x760`，根容器为 `1140x700`，演示区最右控件 `x+w=1112`、最下控件 `y+h=636`，保留超过 20px 逻辑余量。
- 本轮是 Mentions 已完成组件的 Win32 易语言演示补齐，不新增 Element 组件计数；当前组件总数：`91/91`，还剩 `0` 个未完成，完成度 `100.00%`。

### Table 行级增删 API 补齐

- `Table` 新增普通表格行级 CRUD 导出：`EU_AddTableRow`、`EU_InsertTableRow`、`EU_DeleteTableRow`、`EU_ClearTableRows`；行数据复用高级行单行协议，虚表模式返回失败并由回调数据源维护。
- 已同步 C++ 导出声明、`.def`、Python ctypes/helper、易语言/Python/C# DLL 命令文档、Table 组件文档、API 索引、AI SDK 文档和专项测试。
- 本轮是 Table 已完成组件的能力补齐，不新增 Element 组件计数；当前组件总数：`91/91`，还剩 `0` 个未完成，完成度 `100.00%`。

### Dropdown 右键弹层定位修复

- 修复通用 Popup 右键绑定到 `Dropdown` 时，弹层总是按被右键元素左下角定位的问题；右键触发现在会使用鼠标点击点作为临时锚点，更符合右键菜单交互预期。
- 左键、悬停、手动触发仍保持原有元素锚点定位逻辑；`EU_SetPopupAnchorElement` 显式设置锚点时会清除临时点锚点，不影响已有按钮式下拉菜单。
- 已重新生成 `Release|Win32` 的 `new_emoji.dll`，并同步覆盖到 `T:\易语言源码\new_emoji易语言源码\new_emoji.dll`；Dropdown 易语言全功能演示源码继续保持 UTF-8 字节集传参。
- 本轮是 Dropdown 已完成组件的行为修复，不新增 Element 组件计数；当前组件总数：`91/91`，还剩 `0` 个未完成，完成度 `100.00%`。

### Anchor 易语言全功能演示

- 新增 `T:\易语言源码\new_emoji易语言源码\AnchorAllFeaturesDemo.e.txt`，覆盖 `EU_CreateAnchor`、`EU_SetAnchorItems`、`EU_SetAnchorActive`、`EU_GetAnchorActive`、`EU_GetAnchorItemCount`、`EU_SetAnchorTargets`、`EU_SetAnchorOptions`、`EU_SetAnchorScroll` 和 `EU_GetAnchorState`。
- 演示源码遵守易语言交付规则：窗口标题、主标题、按钮、锚点项目、状态回显和核心展示项均使用 UTF-8 字节集 + 长度传入；源码中不直接写 emoji 字符。
- 首次窗口逻辑尺寸为 `1160x760`，根容器为 `1120x700`，演示区最右控件 `x+w=1068`、最下控件 `y+h=646`，保留超过 20px 逻辑余量。
- 根据运行截图修正易语言演示布局：内容说明卡片改为挂载到根容器并使用全局坐标，避免在 Win32 易语言运行时被绘制到左上角并遮挡标题、说明和锚点列表。
- 本轮是 Anchor 已完成组件的 Win32 易语言演示补齐，不新增 Element 组件计数；当前组件总数：`91/91`，还剩 `0` 个未完成，完成度 `100.00%`。

### Backtop 易语言全功能演示

- 新增 `T:\易语言源码\new_emoji易语言源码\BacktopAllFeaturesDemo.e.txt`，覆盖 `EU_CreateBacktop`、`EU_SetBacktopState`、`EU_SetBacktopOptions`、`EU_SetBacktopScroll`、`EU_TriggerBacktop`、`EU_GetBacktopVisible`、`EU_GetBacktopState` 和 `EU_GetBacktopFullState`。
- 演示源码遵守易语言交付规则：窗口标题、主标题、按钮、状态回显和核心展示项均使用 UTF-8 字节集 + 长度传入；源码中不直接写 emoji 字符。
- 首次窗口逻辑尺寸为 `1120x760`，根容器为 `1080x700`，演示区最右控件 `x+w=1048`、最下控件 `y+h=646`，保留超过 20px 逻辑余量。
- 已确认目标交付目录中的 `new_emoji.dll` 导出 Backtop 相关 8 个入口。
- 本轮是 Backtop 已完成组件的 Win32 易语言演示补齐，不新增 Element 组件计数；当前组件总数：`91/91`，还剩 `0` 个未完成，完成度 `100.00%`。

### Tour 易语言全功能演示

- 新增 `T:\易语言源码\new_emoji易语言源码\TourAllFeaturesDemo.e.txt`，覆盖 `EU_CreateTour`、步骤更新、激活项切换、打开/关闭、坐标目标、目标元素绑定、遮罩显示/隐藏、遮罩穿透/拦截、基础状态与完整状态读回。
- 示例遵守易语言交付规则：窗口标题、主标题、按钮、状态提示、Tour 步骤标题/正文和全部 emoji 均使用 UTF-8 字节集 + 长度传入；`.DLL命令` 左侧中文命令名，右侧入口名保持 `EU_` 导出名。
- 首次窗口逻辑尺寸为 `1160x780`，根容器为 `1120x720`；最右控件 `x+w=1060`，最下控件 `y+h=684`，保留超过 20px 逻辑余量。
- 本轮是 Tour 已完成组件的 Win32 易语言演示补齐，不新增 Element 组件计数；当前组件总数：`91/91`，还剩 `0` 个未完成，完成度 `100.00%`。

### Dialog 易语言全功能演示

- 新增 `T:\易语言源码\new_emoji易语言源码\DialogAllFeaturesDemo.e.txt`，覆盖 `EU_CreateDialog`、打开/关闭、标题正文设置、选项和高级选项读写、内容/页脚 slot、按钮设置与程序触发、文本读回、完整状态读回、按钮回调、before-close 拦截与确认关闭。
- 演示源码遵守易语言交付规则：界面中文和 emoji 均使用 UTF-8 字节集 + 长度传入 DLL，`.DLL命令` 左侧中文、右侧保留真实 `EU_` 导出名；窗口 1180x780、根容器 1140x720，首屏控件预留超过 20px 逻辑余量。
- 本轮只新增 Dialog 易语言演示源码，不新增 Element 组件计数；当前组件总数：`91/91`，还剩 `0` 个未完成，完成度 `100.00%`。

### Menu 横向图标文字布局修复

- 修复 `Menu/NavMenu` 横向导航项在同时包含图标、文字、父级展开标记时，仍按纵向菜单预留右侧快捷键区域，导致功能演示区顶部横向导航图标与文字重叠的问题。
- 横向菜单项现在只保留正常右侧内边距，文字从图标后方左对齐绘制；纵向菜单继续保留快捷键与子菜单箭头区域，现有侧边导航、折叠菜单和多级菜单行为不变。
- 已同步 `src/element_menu.cpp`，无新增导出 API，Python ctypes 封装和 DLL 命令文档无需调整；已重新生成 `Release|x64` 与 `Release|Win32` DLL。
- 本轮是 Menu 已完成组件的绘制布局修复，不新增 Element 组件计数；当前组件总数：`91/91`，还剩 `0` 个未完成，完成度 `100.00%`。

### Element Popup 绑定 API

- 新增通用组件弹层绑定能力：`EU_SetElementPopup`、`EU_ClearElementPopup`、`EU_GetElementPopup`，支持任意 Element 按左键、右键、悬停、聚焦、手动触发绑定弹层。
- 通用 Popup 层已扩展到 `Dropdown`：`EU_SetPopupAnchorElement`、`EU_SetPopupPlacement`、`EU_SetPopupOpen`、`EU_GetPopupOpen`、`EU_SetPopupDismissBehavior` 现在统一支持 `Popover`、`Menu`、`Dropdown`。
- `EU_SetIconButtonDropdown` 保持兼容，并复用左键组件弹层绑定机制；打开一个绑定弹层时会关闭同窗口内其他绑定弹层。
- `chrome_shell_demo.py` 新增地址栏、标签栏、内容区右键菜单；`component_gallery.py` 新增“右键弹层绑定 🖱️”演示页，覆盖 Panel/Button/Omnibox 与 Menu/Popover/Dropdown 组合。
- 已同步 C++ 导出、`.def`、Python ctypes/helper、组件文档、API 索引、三份 DLL 命令文档和 `tests/python/test_element_popup_binding_components.py`。
- 本轮是通用弹层服务能力补齐，不新增 Element 组件计数；当前组件总数：`91/91`，还剩 `0` 个未完成，完成度 `100.00%`。

### Popup 通用弹层 API

- 新增通用弹层控制能力：`EU_SetPopupAnchorElement`、`EU_SetPopupPlacement`、`EU_SetPopupOpen`、`EU_GetPopupOpen`、`EU_SetPopupDismissBehavior`，当前支持 `Popover`、`Menu` 和 `Dropdown`。
- `EU_SetIconButtonDropdown` 已强化为点击自动切换绑定 popup；打开一个工具按钮 popup 时会关闭同窗口内其他 popup，并在弹层打开时抑制当前按钮 tooltip。
- `chrome_shell_demo.py` 右侧“下载 / 扩展程序 / 更多”三个工具按钮已改为真实锚定弹层：下载气泡、扩展程序气泡和更多菜单；窗口 resize 后弹层跟随按钮重新定位。
- 已同步 C++ 导出、`.def`、Python ctypes/helper、组件文档、API 索引、三份 DLL 命令文档和 `tests/python/test_popup_components.py`。
- 本轮是通用 Popup 服务能力补齐，不新增 Element 组件计数；当前组件总数：`91/91`，还剩 `0` 个未完成，完成度 `100.00%`。

### Window Frame 通用窗口框架 API

- 新增通用窗口框架能力：`EU_CreateWindowEx`、`EU_GetWindowFrameFlags`、`EU_SetWindowFrameFlags`、窗口缩放边框、拖拽/非拖拽区域和 Element 窗口命令绑定。
- `chrome_shell_demo.py` 已改用通用 `EU_WINDOW_FRAME_BROWSER_SHELL` 组合，不再依赖内置 TitleBar 占位；右上角最小化、最大化/还原、关闭按钮改为 `IconButton + EU_SetElementWindowCommand`。
- 已同步 Python helper、DLL 命令说明、API 索引和 `docs/components/window-frame.md`；本轮是窗口服务能力补齐，不新增 Element 组件计数。
- 当前组件总数：`91/91`，还剩 `0` 个未完成，完成度 `100.00%`。

### Chrome 高仿外壳 API 补齐

- 新增 `IconButton`、`Omnibox`、`BrowserViewport` 三个 Chrome 外壳组件；同步补齐 C++ Element、本体绘制、导出 API、Python ctypes/helper、独立测试、中文 emoji 示例和组件文档。
- `Tabs` 增加 Chrome 标签栏模式；`Menu` / `Popover` 增加 Chrome 菜单与气泡能力；`TitleBar` / `Window` 增加拖拽区、标题栏高度、caption 按钮样式和圆角配置；`Container` 增加显式 Flex 布局；主题新增 `chrome.*` token 和亮色/暗色/隐身/高对比预设。
- 新增 `examples/python/chrome_shell_demo.py`，首屏为中文 emoji Chrome 外壳：标签栏、地址栏、工具栏、三点菜单和内容占位区。
- 本轮体验补齐：`Omnibox` 空值聚焦时也绘制插入光标，并改用 DirectWrite 命中测试定位英文网址光标，补齐选择区、`Ctrl+A/C/X/V` 和右键中文编辑菜单；`IconButton` 使用自身 `tooltip` 文本在 hover 时绘制悬浮提示，用于 Chrome 外壳导航与工具栏按钮。
- 当前组件总数：`91/91`，还剩 `0` 个未完成，完成度 `100.00%`。

### Mentions 中文输入法过滤修复

- 修复 `Mentions` 聚焦时中文输入法提交文本不会进入组件值的问题：窗口过程现在会把 `WM_IME_COMPOSITION/GCS_RESULTSTR` 转发给 `Mentions`，并由组件统一通过 `commit_text` 更新 value、过滤文本、候选匹配和展开状态。
- 普通字符输入也统一走 `commit_text`，保证英文、中文、emoji 提交后的过滤逻辑一致。
- 已同步 `src/element_mentions.*` 和 `src/window_proc.cpp`；本轮未新增导出 API，Python ctypes 封装和文档 API 列表无需调整。
- 当前组件总数：`88/88`，还剩 `0` 个未完成，完成度 `100.00%`。

### Autocomplete 首次点击下拉保持修复

- 修复 `Autocomplete` 在 `trigger_on_focus=True` 时首次点击主输入框会先由焦点打开下拉、又被同一次鼠标抬起立即关闭的问题；首次点击现在会保持建议层打开，已聚焦后的再次点击仍可正常开关。
- 已同步 `src/element_autocomplete.*`，并在 `tests/python/test_autocomplete_complete_components.py` 增加首次点击打开与二次点击关闭的回归验证。
- 当前组件总数：`88/88`，还剩 `0` 个未完成，完成度 `100.00%`。

### ColorPicker 任意色号与连续色域补齐

- `ColorPicker` 已从“预设色块选择”升级为可编辑色号与连续取色：主框聚焦后可直接输入 `#RRGGBB` / `#AARRGGBB`，合法色号立即写入；弹层新增二维色域和色相条，支持点击/拖动选择非预设颜色。
- 透明度模式下十六进制读回使用 `#AARRGGBB`，预设色、透明度条、清空、尺寸、状态读回和变化回调继续保持兼容。
- 已同步 `src/element_colorpicker.*`、`tests/python/test_colorpicker_complete_components.py`、`docs/components/color-picker.md` 和 `component_gallery.py` ColorPicker 专属演示。
- 当前组件总数：`88/88`，还剩 `0` 个未完成，完成度 `100.00%`。

### Select / SelectV2 下拉表项与已选值对齐补齐

- `Select` 新增 `EU_SetSelectOptionAlignment` / `EU_GetSelectOptionAlignment`、`EU_SetSelectValueAlignment` / `EU_GetSelectValueAlignment`，支持下拉表项内容和输入框内已选值分别左对齐、居中、右对齐；多选模式保留左侧勾选列，仅对表项文本区域应用表项对齐。
- `SelectV2` 新增 `EU_SetSelectV2OptionAlignment` / `EU_GetSelectV2OptionAlignment`、`EU_SetSelectV2ValueAlignment` / `EU_GetSelectV2ValueAlignment`，虚拟列表下拉项和输入框内已选值同样支持左对齐、居中、右对齐，并与滚动索引、搜索匹配、禁用项共存。
- 已同步 `src/element_select.*`、`src/element_selectv2.*`、`src/exports.cpp`、`src/new_emoji.def`、Python ctypes/helper、`test_new_emoji.py`、Select/SelectV2 专项测试、易语言 DLL 命令、组件文档、API 索引和 `component_gallery.py` 专属演示页。
- 当前组件总数：`88/88`，还剩 `0` 个未完成，完成度 `100.00%`。

### Menu 导航滚动与图库左侧导航

- `Menu` 已补齐纵向内容溢出时的原生滚动能力：鼠标滚轮、滚动条拖动、轨道点击、键盘 PageUp/PageDown，以及激活项自动滚入可视区。
- `component_gallery.py` 已把左侧“一级分类 / 二级组件”两块按钮面板改为单个纵向 `Menu/NavMenu`，顶部主题切换保留；菜单高度按窗口根容器计算，底部不再被遮住。
- `component_gallery.py` 已修正一级分类点击逻辑：不再由回调强制全部展开，而是按点击项切换展开/收起；展开时进入该分类首个组件，收起时保留当前内容页。
- 已同步 `src/element_menu.*`、组件文档、组件 README、`tests/python/test_menu_complete_components.py` 和 `component_gallery.py`。
- 当前组件总数：`88/88`，还剩 `0` 个未完成，完成度 `100.00%`。

### Container 容器五件套样式补齐

- `Container` 已补齐 Element Plus 风格的 `Container / Header / Aside / Main / Footer` 五件套；`Header`、`Aside`、`Main`、`Footer` 作为一等 C++ 区域组件独立计入组件总数。
- 新增 `EU_CreateHeader`、`EU_CreateAside`、`EU_CreateMain`、`EU_CreateFooter`、`EU_SetContainerLayout`、`EU_GetContainerLayout`、`EU_SetContainerRegionTextOptions`、`EU_GetContainerRegionTextOptions`；`EU_CreateContainer` 默认绝对定位语义保持兼容，流式布局需显式开启。
- 已同步 `src/element_container.*`、`src/exports.*`、`src/new_emoji.def`、Python ctypes/helper、`test_new_emoji.py`、`tests/python/test_container_complete_components.py`、易语言 DLL 命令、组件文档、API 索引、组件 README、组件 manifest、`tools/generate_docs.py` 和 `component_gallery.py` Container 桌面端七布局/后台壳/样式覆盖演示。
- 当前组件总数：`88/88`，还剩 `0` 个未完成，完成度 `100.00%`。

### Collapse 折叠面板全样式能力补齐

- `Collapse` 已补齐 Element Plus 常见折叠面板样式：普通多展开、手风琴、自定义标题图标、右侧说明/图标、禁用项、展开动画、键盘焦点切换和完整状态 JSON。
- 新增 `EU_SetCollapseItemsEx`、`EU_SetCollapseActiveItems`、`EU_GetCollapseActiveItems`、`EU_GetCollapseStateJson`；旧 `EU_CreateCollapse`、`EU_SetCollapseItems`、`EU_SetCollapseActive`、`EU_GetCollapseActive` 保持兼容。
- 已同步 `src/element_collapse.*`、`src/exports.*`、`src/new_emoji.def`、Python ctypes/helper、`test_new_emoji.py`、`tests/python/test_collapse_complete_components.py`、易语言 DLL 命令、组件文档、API 索引、组件 README、组件 manifest 和 `component_gallery.py` Collapse 专属桌面端全样式演示。
- 历史记录时总数：`84/84`，还剩 `0` 个未完成，完成度 `100.00%`。

### Card 卡片全样式能力补齐

- `Card` 已按“原生卡片 + 子元素插槽”补齐 Element UI 常用桌面样式：标题、正文、无头卡片、页脚操作、正文列表、列表间距、列表项上下 padding、分割线、无阴影/常驻阴影/悬停阴影、图片卡片和头部右侧操作按钮。
- 新增 `EU_SetCardTitle`、`EU_SetCardStyle`、`EU_GetCardStyle`、`EU_SetCardBodyStyle`、`EU_GetCardBodyStyle`、`EU_SetCardItems`、`EU_GetCardItemCount`；旧 `EU_CreateCard`、`EU_SetCardBody`、`EU_SetCardFooter`、`EU_SetCardActions`、`EU_SetCardOptions` 保持兼容。
- 已同步 `src/element_card.*`、`src/exports.*`、`src/new_emoji.def`、Python ctypes/helper、`test_new_emoji.py`、`tests/python/test_card_complete_components.py`、易语言 DLL 命令、组件文档、API 索引、组件 README、组件 manifest 和 `component_gallery.py` Card 专属桌面端全样式演示。
- 历史记录时总数：`84/84`，还剩 `0` 个未完成，完成度 `100.00%`。

### Carousel 走马灯全样式补齐

- `Carousel` 已补齐 Element Plus 常用桌面样式：hover/click 指示器触发、inside/outside 指示器位置、箭头 hover/always/never、自动播放 interval、普通/卡片模式、水平/垂直方向、循环边界、项目背景色、文字颜色/字号/透明度。
- 新增 `EU_SetCarouselBehavior`、`EU_GetCarouselBehavior`、`EU_SetCarouselVisual`、`EU_GetCarouselVisual`，旧 `EU_SetCarouselOptions`、`EU_SetCarouselAutoplay`、`EU_SetCarouselAnimation` 保持兼容；旧 `show_arrows` 会映射到新箭头模式。
- 已同步 `src/element_carousel.*`、`src/exports.*`、`src/new_emoji.def`、Python ctypes/helper、`test_new_emoji.py`、`tests/python/test_carousel_complete_components.py`、易语言 DLL 命令、组件文档、API 索引、组件 README 和 `component_gallery.py` Carousel 专属桌面端全样式演示。
- 历史记录时总数：`84/84`，还剩 `0` 个未完成，完成度 `100.00%`。

### ColorPicker 颜色选择器全样式补齐

- `ColorPicker` 已补齐 Element Plus 示例对应样式：有默认值、无默认值/空值态、透明度开关、预设色、默认/中等/小型/超小四种尺寸、可清空、状态读回和变化回调。
- 新增 `EU_SetColorPickerOptions`、`EU_GetColorPickerOptions`、`EU_ClearColorPicker`、`EU_GetColorPickerHasValue`，旧 `EU_CreateColorPicker`、颜色/透明度/hex/open/palette/回调 API 保持兼容。
- 已同步 `src/element_colorpicker.*`、`src/exports.*`、`src/new_emoji.def`、Python ctypes/helper、`test_new_emoji.py`、`tests/python/test_colorpicker_complete_components.py`、易语言 DLL 命令、组件文档、API 索引、组件 README、组件 manifest 和 `component_gallery.py` ColorPicker 专属桌面端全样式演示。
- 历史记录时总数：`84/84`，还剩 `0` 个未完成，完成度 `100.00%`。

### Empty 空状态全样式能力补齐

- `Empty` 已从基础 emoji 图标 + 描述 + 内置按钮升级为完整空状态组件：支持描述文字、自定义图片、本地/HTTP/HTTPS 图片源、图片尺寸、内置操作按钮和真实 Button 子元素插槽。
- 新增 `EU_SetEmptyImage`、`EU_SetEmptyImageSize`、`EU_GetEmptyImageStatus`、`EU_GetEmptyImageSize`，旧 `EU_CreateEmpty`、`EU_SetEmptyDescription`、`EU_SetEmptyOptions`、`EU_SetEmptyActionClicked`、`EU_GetEmptyActionClicked`、`EU_SetEmptyActionCallback` 保持兼容。
- 已同步 `src/element_empty.*`、`src/element_image.*`、`src/exports.*`、`src/new_emoji.def`、Python ctypes/helper、`test_new_emoji.py`、`tests/python/test_empty_complete_components.py`、易语言 DLL 命令、组件文档、API 索引、组件 README、组件 manifest 和 `component_gallery.py` Empty 专属桌面端全样式演示。
- 历史记录时总数：`84/84`，还剩 `0` 个未完成，完成度 `100.00%`。

### Descriptions 描述列表全样式能力补齐

- `Descriptions` 已从基础 label/value 列表升级为完整描述列表组件：支持基础、带边框、无边框、横向、垂直、四种尺寸、逐项跨列、label 图标、内容标签、右上角操作按钮、逐项 label/content 背景色和内容右对齐。
- 新增 `EU_SetDescriptionsLayout`、`EU_SetDescriptionsItemsEx`、`EU_SetDescriptionsColors`、`EU_SetDescriptionsExtra`、`EU_GetDescriptionsFullState`，旧 `EU_CreateDescriptions`、`EU_SetDescriptionsItems`、`EU_SetDescriptionsOptions`、`EU_GetDescriptionsOptions` 保持兼容。
- 已同步 `src/element_descriptions.*`、`src/exports.*`、`src/new_emoji.def`、Python ctypes/helper、`test_new_emoji.py`、`tests/python/test_descriptions_complete_components.py`、易语言 DLL 命令、组件文档、API 索引和 `component_gallery.py` Descriptions 专属桌面端全样式演示。
- 历史记录时总数：`84/84`，还剩 `0` 个未完成，完成度 `100.00%`。

### Statistic 统计数值全样式能力补齐

- `Statistic` 已从基础指标卡补齐为完整统计数值组件：支持普通数值、千分位、小数精度、小数分隔符、前后缀 emoji、前后缀独立颜色、自定义 formatter 文本、后缀点击、倒计时、暂停/继续、增加时间、结束回调和完整状态读回。
- 新增 `EU_SetStatisticNumberOptions`、`EU_SetStatisticAffixOptions`、`EU_SetStatisticDisplayText`、`EU_SetStatisticCountdown`、`EU_SetStatisticCountdownState`、`EU_AddStatisticCountdownTime`、`EU_SetStatisticFinishCallback`、`EU_SetStatisticSuffixClickCallback`、`EU_GetStatisticFullState`，旧 `EU_CreateStatistic`、`EU_SetStatisticValue`、`EU_SetStatisticFormat`、`EU_SetStatisticOptions`、`EU_GetStatisticOptions` 保持兼容。
- 已同步 `src/element_statistic.*`、`src/exports.*`、`src/new_emoji.def`、Python ctypes/helper、`test_new_emoji.py`、`tests/python/test_statistic_complete_components.py`、易语言 DLL 命令、组件文档、API 索引、组件 README 和 `component_gallery.py` Statistic 专属桌面端全样式演示。
- 历史记录时总数：`84/84`，还剩 `0` 个未完成，完成度 `100.00%`。

### Avatar 头像全样式补齐

- `Avatar` 从基础头像升级为完整样式覆盖：支持圆形/方形、50/large/medium/small 示例尺寸、图片/图标/文字/emoji 内容、本地与 http/https 图片源、失败备用图片、错误文本和五种适配方式。
- `fit` 已与 `Image` 对齐为 `0 contain`、`1 cover`、`2 fill`、`3 none`、`4 scale-down`，远程图片复用 Image 的异步下载、临时缓存和窗口刷新逻辑。
- 新增 `EU_SetAvatarFallbackSource`、`EU_SetAvatarIcon`、`EU_SetAvatarErrorText`，并同步 `.def`、Python ctypes/helper、`test_new_emoji.py`、易语言 DLL 命令、组件文档、API 索引、组件 README、`component_gallery.py` Avatar 专属图库页和专项测试。
- 历史记录时总数：`84/84`，还剩 `0` 个未完成，完成度 `100.00%`。

### Alert 全样式补齐

- `Alert` 从基础封装补齐为完整样式覆盖：支持四类型、浅色/深色效果、不可关闭、自定义关闭文字、显示/隐藏图标、居中文案、长中文描述自动换行、多类型带描述和关闭回调。
- 新增 `EU_CreateAlertEx`、`EU_SetAlertAdvancedOptions`、`EU_GetAlertAdvancedOptions`、`EU_SetAlertCloseText`、`EU_GetAlertText`，并保持旧 `EU_CreateAlert` 签名与默认行为兼容。
- 已同步 C++ 导出、`.def`、Python ctypes/helper、易语言 DLL 命令、组件文档、API 索引、`component_gallery.py` Alert 专属完整图库页和专项测试。
- 历史记录时总数：`84/84`，还剩 `0` 个未完成，完成度 `100.00%`。

### Loading 加载全样式能力补齐

- `Loading` 已补齐 Element UI 常见加载样式：局部遮罩、自定义加载文案、自定义遮罩背景、点状旋转、圆弧旋转、脉冲圆点、全屏锁定、服务式显示/关闭和进度显示。
- 新增 `EU_SetLoadingText`、`EU_SetLoadingStyle`、`EU_GetLoadingStyle`、`EU_ShowLoading`、`EU_CloseLoading`，旧 `EU_CreateLoading`、激活/选项/目标/文本/完整状态 API 保持兼容。
- 已同步 `src/element_loading.*`、`src/exports.*`、`src/new_emoji.def`、Python ctypes/helper、`test_new_emoji.py`、`tests/python/test_loading_complete_components.py`、组件文档、API 索引、易语言 DLL 命令和 `component_gallery.py` Loading 专属桌面端全样式演示。
- 历史记录时总数：`84/84`，还剩 `0` 个未完成，完成度 `100.00%`。

### Progress 进度条全样式能力补齐

- `Progress` 已补齐 Element UI `el-progress` 对应样式：条形、文本内显、状态色、自定义颜色、颜色分段、圆形进度和仪表盘进度，`dashboard` 并入原生 Progress。
- 新增 `EU_SetProgressTextInside`、`EU_GetProgressTextInside`、`EU_SetProgressColors`、`EU_GetProgressColors`、`EU_SetProgressColorStops`、`EU_GetProgressColorStopCount`、`EU_GetProgressColorStop`、`EU_SetProgressCompleteText`、`EU_GetProgressCompleteText`、`EU_SetProgressTextTemplate`、`EU_GetProgressTextTemplate`，旧 `EU_CreateProgress`、百分比/状态/选项/格式 API 保持兼容。
- 文本格式扩展为百分比、中文状态、标签、满值文本、模板文本；颜色分段支持 `颜色<TAB>百分比|颜色<TAB>百分比` UTF-8 协议，并按百分比升序使用。
- 已同步 `src/element_progress.*`、`src/exports.*`、`src/new_emoji.def`、Python ctypes/helper、`test_new_emoji.py`、`tests/python/test_progress_complete_components.py`、组件文档、API 索引、组件 README、易语言 DLL 命令和 `component_gallery.py` Progress 专属桌面端全样式演示。
- 历史记录时总数：`84/84`，还剩 `0` 个未完成，完成度 `100.00%`。

### Pagination 分页全样式能力补齐

- `Pagination` 已补齐 Element Plus 对应样式：基础分页、页数较少、大页数省略、`pager-count`、背景分页、小尺寸、显示总数、每页数量、跳转页、完整功能和单页隐藏。
- 新增 `EU_SetPaginationAdvancedOptions`、`EU_GetPaginationAdvancedOptions`，旧 `EU_CreatePagination`、`EU_SetPaginationOptions`、`EU_SetPaginationPageSizeOptions`、状态读回和回调接口保持兼容。
- 已同步 `src/element_pagination.*`、`src/exports.*`、`src/new_emoji.def`、Python ctypes/helper、`test_new_emoji.py`、`tests/python/test_pagination_complete_components.py`、组件文档、API 索引、组件 README、易语言 DLL 命令和 `component_gallery.py` Pagination 桌面端全样式演示。
- 历史记录时总数：`84/84`，还剩 `0` 个未完成，完成度 `100.00%`。

### Divider 分割线全样式能力补齐

- `Divider` 已补齐默认横线、横向文字左/中/右、图标/emoji 内容、竖向分割线、实线/虚线/点线/双线、颜色、线宽、外边距和文本间隙。
- 新增 `EU_SetDividerLineStyle`、`EU_GetDividerLineStyle`、`EU_SetDividerContent`、`EU_GetDividerContent`，旧 `EU_SetDividerOptions(... dashed, text)` 保持兼容，`dashed=True` 映射为虚线线型。
- 已同步 `src/element_divider.*`、`src/exports.*`、`src/new_emoji.def`、Python ctypes/helper、`test_new_emoji.py`、`tests/python/test_divider_complete_components.py`、组件文档、API 索引、组件 README、组件 manifest、易语言 DLL 命令和 `component_gallery.py` Divider 专属桌面端全样式演示。
- 历史记录时总数：`84/84`，还剩 `0` 个未完成，完成度 `100.00%`。

### Calendar 日历全样式能力补齐

- `Calendar` 已补齐 Element UI 对应能力：基础值绑定、自定义 `dateCell` 单元格内容/样式、`:range` 显示范围、可选范围限制、区间选择读回、键盘导航、选中标记和变化回调。
- 新增 `EU_SetCalendarDisplayRange`、`EU_GetCalendarDisplayRange`、`EU_SetCalendarCellItems`、`EU_GetCalendarCellItems`、`EU_ClearCalendarCellItems`、`EU_SetCalendarVisualOptions`、`EU_GetCalendarVisualOptions`、`EU_SetCalendarStateColors`、`EU_GetCalendarStateColors`、`EU_SetCalendarSelectedMarker`、`EU_SetCalendarChangeCallback`，旧 `EU_CreateCalendar`、`EU_SetCalendarRange`、`EU_SetCalendarSelectionRange` 等接口保持兼容。
- 单元格数据采用 UTF-8 文本协议：每行一个日期、字段用 Tab 分隔，支持 `label/extra/emoji/badge/bg/fg/border/badge_bg/badge_fg/font_flags/disabled`，可对单个日期覆盖样式或禁用点击。
- 已同步 `src/element_calendar.*`、`src/exports.*`、`src/new_emoji.def`、Python ctypes/helper、`test_new_emoji.py`、`tests/python/test_calendar_complete_components.py`、组件文档、API 索引、组件 README、易语言 DLL 命令和 `component_gallery.py` Calendar 专属桌面端全样式演示。
- 历史记录时总数：`84/84`，还剩 `0` 个未完成，完成度 `100.00%`。

### Timeline 时间线全样式能力补齐

- `Timeline` 已补齐 Element UI 对应样式：基础时间线、正序/倒序、节点图标、类型色、自定义颜色、大尺寸节点、顶部/底部时间戳、卡片式时间线、左侧/右侧/交替位置。
- 新增 `EU_SetTimelineAdvancedOptions`、`EU_GetTimelineAdvancedOptions`，旧 `EU_CreateTimeline`、`EU_SetTimelineItems`、`EU_SetTimelineOptions`、`EU_GetTimelineOptions` 保持兼容。
- 项目数据格式扩展为 `时间<TAB>内容<TAB>类型<TAB>图标<TAB>颜色<TAB>尺寸<TAB>时间位置<TAB>卡片标题<TAB>卡片正文`，支持 `#RRGGBB`、`0xAARRGGBB`、`normal/large`、`top/bottom`，`el-icon-more` 映射为 `⋯`。
- 已同步 `src/element_timeline.*`、`src/exports.*`、`src/new_emoji.def`、Python ctypes/helper、`test_new_emoji.py`、`tests/python/test_timeline_complete_components.py`、组件文档、API 索引、组件 README、易语言 DLL 命令和 `component_gallery.py` Timeline 专属桌面端全样式演示。
- 历史记录时总数：`84/84`，还剩 `0` 个未完成，完成度 `100.00%`。

### Steps 步骤条全样式能力补齐

- `Steps` 已补齐 Element UI 对应样式：基础步骤条、固定间距 `space`、长描述、`align-center` 居中、自定义图标、纵向步骤条、`simple` 简洁模式，以及等待/进行中/完成/失败状态。
- 新增 `EU_SetStepsIconItems`、`EU_SetStepsOptions`、`EU_GetStepsOptions`、`EU_GetStepsVisualState`，旧 `EU_CreateSteps`、`EU_SetStepsItems`、`EU_SetStepsDetailItems`、`EU_SetStepsStatuses` 等接口保持兼容。
- 已同步 `src/element_steps.*`、`src/exports.*`、`src/new_emoji.def`、Python ctypes/helper、`test_new_emoji.py`、`tests/python/test_steps_complete_components.py`、组件文档、API 索引、组件 README、易语言 DLL 命令和 `component_gallery.py` Steps 专属桌面端全样式演示。
- 历史记录时总数：`84/84`，还剩 `0` 个未完成，完成度 `100.00%`。

### Image 图片全样式能力补齐

- `Image` 已补齐 Element UI 图片组件对应能力：`fill / contain / cover / none / scale-down` 五种 fit、本地路径、HTTP/HTTPS 远程图片异步加载、默认/自定义占位、默认/自定义失败、视口懒加载和预览图片列表。
- 新增 `EU_SetImageLazy`、`EU_SetImagePlaceholder`、`EU_SetImageErrorContent`、`EU_SetImagePreviewList`、`EU_SetImagePreviewIndex`、`EU_GetImageAdvancedOptions`，旧 `EU_CreateImage`、`EU_SetImageSource`、`EU_SetImageFit`、`EU_GetImageOptions`、`EU_GetImageFullOptions` 保持兼容。
- 预览层已支持上一张/下一张、键盘左右切换、Esc 关闭，原有缩放百分比和偏移继续保留。
- 已同步 `src/element_image.*`、`src/element_tree.cpp`、`src/exports.*`、`src/new_emoji.def`、`new_emoji.vcxproj`、Python ctypes/helper、`test_new_emoji.py`、`tests/python/test_image_complete_components.py`、组件文档、API 索引、组件 README、组件 manifest、易语言 DLL 命令和 `component_gallery.py` Image 专属桌面端全样式演示。
- 历史记录时总数：`84/84`，还剩 `0` 个未完成，完成度 `100.00%`。

### Tree / TreeSelect JSON 高级全样式完成

- 本轮修复 Tree 根节点右侧 `tag/actions` 与主文本重叠：绘制时会从右向左预留操作按钮、状态标签、懒加载徽标空间，主文本只在剩余区域内裁切显示；TreeSelect 弹层节点标签也同步预留宽度。
- 本轮修复新增节点后无法完全收缩：过滤状态下仍遵守祖先展开状态，根节点折叠后不会继续露出匹配/新增子节点；普通追加节点不再强制展开父节点，仅懒加载回填时保持父节点展开。
- `Tree` 已切换为推荐 JSON UTF-8 公共 API，覆盖嵌套 children、props 字段映射、复选框级联/半选、严格勾选、懒加载、叶子节点、默认展开/勾选、禁用节点、按 key 读写、追加/更新/删除、过滤、手风琴、拖拽和桌面端内置自定义节点模板。
- `TreeSelect` 已同步补齐 JSON 数据模型和高级状态读写，支持单选/多选 selectedKeys、搜索 searchText、弹层 open、清空、懒加载、禁用节点、props 映射、追加/更新/删除、拖拽回调和状态 JSON 读回。
- 新增并同步 `EU_SetTreeDataJson` / `EU_GetTreeDataJson` / `EU_SetTreeOptionsJson` / `EU_GetTreeStateJson` / keys JSON / 节点 JSON / 回调 API，以及 TreeSelect 对应 JSON API；旧扁平索引接口保留为兼容入口。
- 已同步 `src/element_treeview.*`、`src/element_treeselect.*`、`src/element_types.h`、`src/exports.*`、`src/new_emoji.def`、Python ctypes/helper、`test_new_emoji.py`、Tree/TreeSelect 专项测试、组件文档、API 索引、组件 README、易语言 DLL 命令和 `component_gallery.py` 专属演示页。
- `component_gallery.py` 已为 Tree 和 TreeSelect 新增桌面端专属完整演示：左侧操作区、右侧组件区、底部状态读回，覆盖基础嵌套、props、复选/半选、懒加载、叶子节点、默认 keys、禁用项、key 读写、追加删除、过滤、手风琴、拖拽和自定义节点模板。
- 历史记录时总数：`84/84`，还剩 `0` 个未完成，完成度 `100.00%`。

### Transfer 全样式能力补齐

- `Transfer` 已从简单文本列表升级为结构化数据模型，覆盖 `key / label / value / desc / pinyin / disabled / checked`，旧 `EU_CreateTransfer` / `EU_SetTransferItems` 继续兼容。
- 新增扩展数据、样式选项、标题、按钮文字、统计格式、字段模板、底部操作区、筛选占位、默认勾选、目标值读回和文本读回等 API，并同步 `.def`、C++ 导出、Python ctypes/helper、`test_new_emoji.py` 和易语言 DLL 命令。
- 绘制已补齐桌面端面板头部、复选框行、禁用态、hover/pressed/focus、筛选输入框、空状态、统计格式、左右/全部移动按钮和底部操作区，默认界面文案保持中文。
- `component_gallery.py` 已新增 Transfer 专属样式页，覆盖基础穿梭、拼音筛选、自定义项模板、自定义标题/按钮/统计、字段映射、footer 操作和状态读回；专项测试 `tests/python/test_transfer_complete_components.py` 已同步更新。
- 历史记录时总数：`84/84`，还剩 `0` 个未完成，完成度 `100.00%`。

### Badge 全样式能力补齐

- `Badge` 已补齐数字徽标、最大值截断、`new` / `hot` 文本徽标、dot 圆点、四角位置、独立徽标、按钮附着徽标、下拉菜单 `mark` 组合和桌面端密集样式演示。
- 新增 `EU_SetBadgeType` / `EU_GetBadgeType`，支持默认、主色、成功、警告、信息五类语义配色；`EU_SetElementColor` 继续作为任意自定义色覆盖入口。
- 本轮复查截图后修正按钮附着徽标与角标锚点：`Button` 现在会绘制 Badge 子元素，Badge 默认鼠标穿透避免遮挡父按钮点击；非独立徽标按内容宽度贴近文字，左侧角标会自动给文字让位，不再按整块演示宽度漂移或压住标签。
- `component_gallery.py` 的 Badge 页已给两个按钮和下拉菜单补齐真实交互回显，点击按钮会更新徽标数值；下拉选择会同步更新触发按钮文案、状态行和右侧 `mark` 徽标组合。
- 已同步 `src/element_badge.cpp/.h`、`src/exports.cpp/.h`、`src/new_emoji.def`、Python ctypes/helper、`test_new_emoji.py`、易语言 DLL 命令、组件文档、API 索引、组件 README、`component_gallery.py` 和专项测试 `tests/python/test_badge_complete_components.py`。
- 历史记录时总数：`84/84`，还剩 `0` 个未完成，完成度 `100.00%`。

### Tabs 全样式封装完成

- `Tabs` 已补齐默认线形、卡片、边框卡片、四方向位置、表头左/中/右对齐、图标标签、禁用项、可关闭、可新增、editable、内容区绘制、按名称激活、扩展项目读回和完整状态读回。
- 已同步 `src/element_tabs.cpp/.h`、`src/exports.cpp/.h`、`src/new_emoji.def`、Python ctypes/helper、`tests/python/test_tabs_complete_components.py`、`docs/components/tabs.md`、`docs/api-index.md`、`DLL命令/易语言DLL命令.md`、`examples/python/component_gallery.py`。
- `component_gallery.py` 已新增 Tabs 专属桌面工作台演示，覆盖顶部/底部/左右、表头居中、图标标签、可编辑、多文档新增/关闭和内容区场景。
- 历史记录时总进度：`84/84`，还剩 `0` 个未完成，完成度 `100.00%`。

### Menu / NavMenu 全样式补齐

- `Menu` 已补齐 NavMenu 导航菜单桌面端常见样式：横向默认、横向暗色/自定义色、纵向默认、纵向暗色/自定义色、多级子菜单、禁用项、分组标题、图标、链接项和侧栏展开/收起。
- 新增 `EU_SetMenuColors`、`EU_GetMenuColors`、`EU_SetMenuCollapsed`、`EU_GetMenuCollapsed`、`EU_SetMenuItemMeta`、`EU_GetMenuItemMeta`、`EU_SetMenuSelectCallback`，并同步 `.def`、C++ 导出、Python ctypes/helper、`test_new_emoji.py`、易语言 DLL 命令、组件文档、API 索引和 `component_gallery.py`。
- `component_gallery.py` 已新增 Menu 专属桌面演示页，覆盖顶部横向导航、侧边导航、分组多级菜单、收起侧栏、链接元数据和状态读回。
- 历史记录时总数：`84/84`，还剩 `0` 个未完成，完成度 `100.00%`。

### Drawer 抽屉全样式能力补齐

- `Drawer` 已补齐四方向、无标题栏、50% 百分比尺寸、内容 slot、页脚 slot、真实表格、真实表单、before-close 关闭确认、外层/内层抽屉和桌面属性面板风格。
- 新增 `EU_SetDrawerAdvancedOptions`、`EU_GetDrawerAdvancedOptions`、`EU_GetDrawerContentParent`、`EU_GetDrawerFooterParent`、`EU_SetDrawerBeforeCloseCallback`、`EU_ConfirmDrawerClose`，并同步 `.def`、C++ 导出、Python ctypes/helper、`test_new_emoji.py`、易语言 DLL 命令、组件文档、API 索引和 `component_gallery.py`。
- `component_gallery.py` 已把 `Drawer` 升级为专属桌面端抽屉工作台，覆盖方向与尺寸、无标题栏、嵌套表格、嵌套表单、关闭确认、外层/内层抽屉和自定义风格。
- 新增/扩展 `tests/python/test_drawer_complete_components.py`，覆盖四方向、像素/百分比尺寸、header/close/ESC 开关、slot 挂载、before-close pending/取消/确认、关闭回调和嵌套抽屉。
- 历史记录时总数：`84/84`，还剩 `0` 个未完成，完成度 `100.00%`。

### Dropdown 全样式能力补齐

- `Dropdown` 已补齐文字链接、按钮、split-button、hover/click/manual、`hide_on_click`、图标、禁用、分割线、层级、`command` 回调和四种尺寸。
- 新增 `EU_SetDropdownOptions`、`EU_GetDropdownOptions`、`EU_SetDropdownItemMeta`、`EU_GetDropdownItemMeta`、`EU_SetDropdownCommandCallback`、`EU_SetDropdownMainClickCallback`，并同步 `.def`、Python ctypes/helper、`test_new_emoji.py`、易语言 DLL 命令、组件文档、API 索引和 `component_gallery.py`。
- `component_gallery.py` 已把 `Dropdown` 升级为专属桌面端样式页，覆盖文字链接、默认按钮、主色按钮、hover/click、split-button、hide-on-click=false、command 回显、图标项、禁用项、分割线、层级、长列表滚动和四种尺寸。
- 历史记录时总数：`84/84`，还剩 `0` 个未完成，完成度 `100.00%`。

## 鏈疆淇

### Rate 全样式能力补齐

- `Rate` 补齐三段颜色、Unicode/emoji 图标、逐项评分文案、`show-text`、`show-score`、`text-color` 和 `{value}/{max}` 分数模板。
- 新增 `EU_SetRateColors`、`EU_GetRateColors`、`EU_SetRateIcons`、`EU_GetRateIcons`、`EU_SetRateTextItems`、`EU_SetRateDisplayOptions`、`EU_GetRateDisplayOptions`，并同步 `.def`、Python ctypes/helper、`test_new_emoji.py`、易语言 DLL 命令、组件文档、API 索引和 `component_gallery.py`。
- `component_gallery.py` 已把 `Rate` 从紧凑演示升级为专属桌面端样式页，覆盖默认不区分颜色、区分颜色、显示文字、表情图标、禁用分数、半星、可清空、只读、10 分制和自定义模板。
- 历史记录时总数：`84/84`，还剩 `0` 个未完成，完成度 `100.00%`。

### Table 高级样式与单元格控件补齐

- `Table` 从基础表格升级为高级样式完成：补齐基础/斑马/边框、固定表头、最大高度、横向滚动、固定左右列、多级表头、行/单元格颜色、对齐、字体标志和字号覆盖。
- 补齐单选/多选、索引列、排序、筛选、搜索、展开行、树形行、懒加载标记、合计行、自定义合计、行列合并，以及 `text/index/selection/expand/button/buttons/combo/switch/select/progress/status/tag/popover_tag` 内置单元格类型。
- 新增 `EU_SetTableColumnsEx`、`EU_SetTableRowsEx`、`EU_SetTableCellEx`、样式/选择/筛选/搜索/合并/合计/树形/视口/滚动/回调/读回等高级导出，并同步 `new_emoji.def`、Python ctypes/helper、易语言 DLL 命令、组件文档、API 索引、组件 README、`component_gallery.py` Table 专属图库页和专项测试。
- 新增 `tests/python/test_table_complete_components.py`；`tests/python/test_data_display_table_complete_components.py` 保留旧基础表格回归并增加高级状态读回。
- 本轮修复 Table 图库显示混乱：C++ 绘制按固定左列、滚动区、固定右列分层裁剪，单元格内容不再越界覆盖相邻列；横向滚动会按真实可滚宽度钳制；多级表头不再把无父级标题的连续列误合并。`component_gallery.py` 同步放宽列宽、行高和演示面板高度，减少大面积空白与控件挤压。
- 追加修复选择框与组合框交互：`Selection` 复选框状态会跟随选中行同步，`Select` / `Combo` 现在显示当前值而不是把全部选项平铺出来，并支持点击弹出下拉选择；按钮组文字与背景宽度也已重新匹配。
- 历史记录时总数：`84/84`，还剩 `0` 个未完成，完成度 `100.00%`。

### Table 滚动条与单元格点击回调

- `Table` 补齐原生纵向滚动条与横向滚动条绘制，内容溢出时显示轨道和滑块，支持鼠标拖动滑块、点击轨道翻页、滚轮纵向滚动和 `EU_SetTableScroll` 程序化滚动。
- 横向滚动条会避开固定列、固定右列和合计行，表格内容区会扣除滚动条高度，避免下拉项、最后一行和滚动条互相遮挡。
- 新增 `EU_SetTableCellClickCallback`，任意单元格点击都会独立触发点击回调；原 `EU_SetTableCellActionCallback` 继续负责按钮、开关、选择框、展开、排序等动作语义。
- 已同步 `new_emoji.def`、C++ 导出、Python ctypes/helper、易语言 DLL 命令、组件文档、API 索引、组件 README、`component_gallery.py` Table 样式图库和专项测试。
- 历史记录时总数：`84/84`，还剩 `0` 个未完成，完成度 `100.00%`。

### Table 表头拖拽与 Excel

- 新增 `EU_SetTableHeaderDragOptions`，支持列宽拖拽和表头总高度拖拽开关，关闭后完全禁用拖拽命中。
- 新增 `EU_ExportTableExcel` / `EU_ImportTableExcel`，把 Table 的多行表头和叶子列数据导入导出到 `.xlsx`。
- `component_gallery.py` 已补齐 “表头拖拽 + Excel” 演示分组，和其它 Table 样式一起作为桌面端习惯示例展示。
- 历史记录时总数：`84/84`，还剩 `0` 个未完成，完成度 `100.00%`。

### Dialog 鐐瑰嚮鍛戒腑淇

- 淇 `footer_center=True` 鏃堕粯璁ら〉鑴氭寜閽粯鍒跺眳涓絾鍛戒腑浠嶆寜鍙冲榻愯绠楃殑闂锛岄粯璁ゆ寜閽拰 footer slot 鐨勫懡涓笌缁樺埗淇濇寔涓€鑷淬€?- 浼樺寲 `component_gallery.py` 鐨?`纭鍏抽棴` 绀轰緥锛氬凡鏈?before-close 鎷︽埅鏃剁户缁叧闂紝鏈繘鍏ユ嫤鎴椂鍏堣Е鍙戞嫤鎴啀纭銆?- `tests/python/test_dialog_complete_components.py` 鏂板榧犳爣鐐瑰嚮灞呬腑椤佃剼鎸夐挳鍥炲綊瑕嗙洊銆?- 褰撳墠鎬昏繘搴︿笉鍙橈細`84/84`锛岃繕鍓?`0` 涓湭瀹屾垚锛屽畬鎴愬害 `100.00%`銆?
### Dialog / Tooltip / Popover / Popconfirm

- 鏈疆鑼冨洿鏄€滄牱寮忎笌 slot 鑳藉姏琛ラ綈鈥濓紝涓嶆敼鍙樻€荤粍浠舵暟锛屼粛鎸?`84/84` 缁熻銆?- `Dialog` 琛ラ綈鍐呭 slot銆侀〉鑴?slot銆佺櫨鍒嗘瘮瀹藉害銆佹爣棰?椤佃剼灞呬腑銆乥efore-close 鎷︽埅涓庣‘璁ょ户缁叧闂€?- `Tooltip` 琛ラ綈 12 鏂逛綅銆乨ark/light銆乨isabled銆佺澶淬€乷ffset銆佹渶澶у搴︺€佸琛屽唴瀹瑰拰鏃у洓鏂逛綅鍏煎銆?- `Popover` 琛ラ綈鍐呭 slot銆?2 鏂逛綅銆乭over/click/focus/manual銆佸閮ㄥ叧闂€佺澶淬€乷ffset 鍜岄敭鐩樺叧闂€?- `Popconfirm` 琛ラ綈 12 鏂逛綅銆佽Е鍙戞柟寮忋€佽嚜瀹氫箟纭/鍙栨秷鎸夐挳銆佽嚜瀹氫箟鍥炬爣鏂囨湰/emoji銆佸浘鏍囬鑹层€侀殣钘忓浘鏍囥€佺粨鏋滃洖璋冨拰绋嬪簭瑙﹀彂缁撴灉銆?- `component_gallery.py` 鐨?Dialog銆乀ooltip銆丳opover銆丳opconfirm 宸叉敼涓哄畬鏁存牱寮忔紨绀洪〉锛屽苟浣跨敤鐪熷疄 slot 瀹瑰櫒鎸傝浇 Table/Input/Select/Button/Text銆?
### Message

- `component_gallery.py` 鏀逛负 Message 涓撳睘婕旂ず椤碉紝鏅€氭寜閽垎鍒Е鍙戜俊鎭€佹垚鍔熴€佽鍛娿€侀敊璇€佸瘜鏂囨湰鍜岃嚜鍔ㄥ叧闂ず渚嬨€?- 淇濈暀鈥滀竴閿爢鍙犲洓绫烩€濅綔涓烘槑纭殑鍫嗗彔鍦烘櫙婕旂ず锛岄伩鍏嶆櫘閫氱ず渚嬫寜閽瘡娆￠兘寮瑰嚭鍥涙潯娑堟伅銆?- 淇 Message 鍥炬爣涓庢鏂囩殑鍨傜洿灞呬腑缁樺埗锛屼紭鍖栨埅鍥句腑娑堟伅鍐呭鍋忎笂鐨勯棶棰樸€?
### MessageBox

- `component_gallery.py` 鏀逛负 MessageBox 涓撳睘婕旂ず椤碉紝鍒嗗埆灞曠ず鎻愮ず妗嗐€佺‘璁ゆ銆佽緭鍏ユ銆侀€氱敤妗嗐€佺被鍨嬪浘鏍囥€佸眳涓€佸瘜鏂囨湰銆佸尯鍒嗗叧闂€佽緭鍏ユ牎楠屽拰寤惰繜鍏抽棴銆?- 淇 prompt 杈撳叆妗嗘枃瀛楀瀭鐩村眳涓粯鍒讹紝閬垮厤鍗犱綅鏂囧瓧鎴栬緭鍏ュ€艰创杩戣緭鍏ユ涓婃部銆?- 琛ラ綈 prompt 杈撳叆妗嗙殑鍏夋爣銆侀€夊尯銆丆trl+A 鍏ㄩ€夈€佹柟鍚戦敭/Home/End銆侀紶鏍囩偣鍑诲畾浣嶃€佹嫋鎷介€夋嫨銆佸壀鍒?澶嶅埗/绮樿创/鍏ㄩ€夊彸閿彍鍗曪紱鍛戒腑鍖哄煙鍜屾枃鏈粯鍒剁户缁娇鐢ㄦ寜 DPI 缂╂斁鍚庣殑鐗╃悊鍧愭爣銆?
## 鏈疆瀹屾垚椤?
### Dialog / Tooltip / Popover / Popconfirm 鏍峰紡涓?slot 鑳藉姏

鐘舵€侊細`宸插畬鎴恅

鏈疆琛ラ綈锛?
- C++ Element 鏈綋銆佸鍑?API銆乣.def` 瀵煎嚭琛ㄣ€?- Python ctypes/helper銆乣test_new_emoji.py` 鍏煎灏佽鍜?4 涓嫭绔嬪畬鏁寸粍浠舵祴璇曘€?- 鏄撹瑷€ DLL 鍛戒护鏂囨。銆佺粍浠舵枃妗ｃ€丄PI 绱㈠紩銆佺粍浠?README 鍜?component_gallery銆?
### Message

鐘舵€侊細`宸插畬鎴恅

鏈疆琛ラ綈锛?
- 鍘熺敓 `Message` Element 涓?`EU_ShowMessage` 鏈嶅姟寮忓叆鍙ｃ€?- 鏅€氭秷鎭€乻uccess/warning/error/info 鍥涚被鍨嬨€佸彲鍏抽棴銆佸眳涓€佽交閲?HTML/VNode 绛変环瀵屾枃鏈€?- 鑷姩鍏抽棴銆乣duration=0`銆侀《閮ㄥ亸绉汇€佸鏉″爢鍙犮€佸叧闂洖璋冦€佸畬鏁寸姸鎬佽鍥炪€?- Python ctypes/helper銆佹槗璇█ DLL 鍛戒护鏂囨。銆佺粍浠舵枃妗ｃ€丄PI 绱㈠紩銆乧omponent_gallery 鍜岀嫭绔嬩腑鏂?emoji 娴嬭瘯銆?
### MessageBox

鐘舵€侊細`宸插畬鎴恅

鏈疆琛ラ綈锛?
- 淇濈暀鍏煎 `EU_ShowMessageBox`銆乣EU_ShowConfirmBox`锛屾柊澧?`EU_ShowMessageBoxEx`銆乣EU_ShowPromptBox`銆?- alert銆乧onfirm銆乸rompt銆乵sgbox銆佺被鍨嬪浘鏍囥€佸眳涓€佽交閲忓瘜鏂囨湰銆佸尯鍒?cancel/close銆?- prompt 杈撳叆鍊笺€佸崰浣嶃€佹鍒欐牎楠屻€侀敊璇彁绀恒€佺‘璁ゆ寜閽?loading/寤惰繜鍏抽棴銆佹墿灞曞洖璋冦€?- prompt 杈撳叆妗嗘敮鎸侀敭鐩橀€夊尯銆侀紶鏍囧畾浣嶃€佸彸閿彍鍗曞拰 DPI 缂╂斁鍛戒腑娴嬭瘯銆?- Python ctypes/helper銆佹槗璇█ DLL 鍛戒护鏂囨。銆佺粍浠舵枃妗ｃ€丄PI 绱㈠紩銆乧omponent_gallery 鍜岀嫭绔嬩腑鏂?emoji 娴嬭瘯銆?
### Notification

鐘舵€侊細`宸插畬鎴恅

鏈疆琛ラ綈锛?
- 淇濈暀宓屽叆寮?`EU_CreateNotification`锛屾柊澧炴湇鍔″紡 `EU_ShowNotification`銆?- 榛樿鑷姩鍏抽棴銆乣duration=0`銆佸洓绫诲瀷銆佸洓瑙?position銆乷ffset銆侀殣钘忓叧闂寜閽€佽交閲忓瘜鏂囨湰鍜屽爢鍙犮€?- `EU_SetNotificationPlacement`銆乣EU_SetNotificationRichMode`銆乣EU_GetNotificationFullStateEx`銆?- Python ctypes/helper銆佹槗璇█ DLL 鍛戒护鏂囨。銆佺粍浠舵枃妗ｃ€丄PI 绱㈠紩銆乧omponent_gallery 鍜岀嫭绔嬩腑鏂?emoji 娴嬭瘯銆?
## 楠岃瘉

- C++ 鏋勫缓锛?  - `Release|x64` 閫氳繃
  - `Release|Win32` 閫氳繃
- Python 娴嬭瘯锛?  - `tests/python/test_dialog_complete_components.py` 閫氳繃
  - `tests/python/test_tooltip_complete_components.py` 閫氳繃
  - `tests/python/test_popover_complete_components.py` 閫氳繃
  - `tests/python/test_popconfirm_complete_components.py` 閫氳繃
  - `tests/python/test_message_complete_components.py` 閫氳繃
  - `tests/python/test_messagebox_complete_components.py` 閫氳繃
  - `tests/python/test_notification_complete_components.py` 閫氳繃
  - `tests/python/test_feedback_components.py` 閫氳繃
  - `tests/python/test_interaction_api_components.py` 閫氳繃
  - `tests/python/test_new_emoji.py` 閫氳繃
  - `examples/python/component_gallery.py` 浠?`NEW_EMOJI_GALLERY_PAGE=feedback` 鐭椂鍚姩閫氳繃

## 褰撳墠缁撹

褰撳墠鎬昏繘搴︿负 `84/84`锛岃繕鍓?`0` 涓湭瀹屾垚锛屽畬鎴愬害 `100.00%`銆?

## Tooltip 鏂逛綅淇

- 淇 Tooltip / Popover / Popconfirm 鐨勫脊鍑虹煩褰㈣鍓洰鏍囷細浠庣埗瀹瑰櫒鏀逛负鏍圭獥鍙ｅ彲瑙嗗尯锛岄伩鍏嶅乏涓娿€佸乏涓嬨€佸彸涓娿€佸彸涓嬬瓑鏂逛綅鍦ㄥ祵濂楀鍣ㄩ噷琚敊璇尋鍋忋€?
- Tooltip 文字尺寸改为 DirectWrite 实测，长文本换行后会按真实高度回算方位，避免 corner placement 在多行内容下看起来漂移。

## Table 视觉与交互再修

- 进一步加大所有 Table 示例的行高与表头高度，避免整体过于紧凑。
- 修正标签列居中、操作按钮背景与文字比例、按钮组居中布局。
- 调整 Table 默认行高/表头高度，减少未显式传参时的压缩感。

## Table 交互微调

- Table 的按钮单元格补上了鼠标悬停描边反馈。
- 单元格控件页的进度列加宽到 200。
- 标签 pill 继续按单元格居中布局并提高最小宽度，减少视觉偏左。

## Table 视觉再收紧

- 单元格控件页的进度列继续加宽到 240，并收紧进度条内部留白。
- 标签 pill 改为更紧凑的宽度估算，并保持列内居中。

## InfiniteScroll 替换 Scrollbar

状态：`已完成`

本轮补齐：
- 公开 `Scrollbar` 组件已硬删除，`EU_CreateScrollbar` 与全部 `EU_*Scrollbar*` 导出已移除。
- 新增原生 `InfiniteScroll` Element：内置列表数据、触底阈值、`loading` / `no_more` / `disabled` 状态、状态文案和竖向滚动指示条。
- 已支持鼠标滚轮、滚动指示条拖动、轨道点击、`Home` / `End` / `PageUp` / `PageDown` / 上下键。
- 已补 `EU_CreateInfiniteScroll`、项目设置/追加/清空、状态、选项、文本、滚动、完整状态读回和触底加载回调。
- 已同步 C++ 工程、`.def`、导出头/实现、Python ctypes/helper、组件文档、组件 README、manifest、API 索引、易语言 DLL 命令文档、测试和 `component_gallery.py`。
- `component_gallery.py` 已放入“数据展示”分类，提供桌面端完整样式演示：基础无限追加、加载中、禁用、没有更多、空状态、卡片列表、紧凑业务列表和状态读回。
- `Table` 内部私有滚动条保留，不再作为公开 `Scrollbar` 组件统计。

历史记录时总进度：`84/84`，还剩 `0` 个未完成，完成度 `100.00%`。

## 多行输入滚动条补齐

状态：`已完成`

本轮补齐：
- `EditBox` 多行模式新增垂直滚动状态、内容高度/视口高度读回、滚轮滚动、滑块拖动、轨道翻页和光标自动滚入视口；单行模式保持原有横向滚动行为。
- `Input` 多行模式新增垂直滚动状态和滚动条绘制；固定高度溢出、`autosize=false`、`autosize=true + max_rows` 限制均支持滚动，继续保留 clear、password toggle、prefix/suffix、word limit、右键菜单等现有行为。
- 新增 `EU_SetEditBoxScroll`、`EU_GetEditBoxScroll`、`EU_SetInputScroll`、`EU_GetInputScroll`，并同步 C++ 导出、`.def`、Python ctypes/helper、`test_new_emoji.py`、易语言/Python/C# DLL 命令文档、API 索引和组件文档。
- `BacktopAllFeaturesDemo.e.txt` 左侧改为真实多行 `Input` 长内容演示，按钮同步调用 Input 滚动 API 和 Backtop 滚动 API；触发回顶后输入框滚动位置回到 `0`。

历史记录时总进度：`91/91`，还剩 `0` 个未完成，完成度 `100.00%`。

