# C# WinForms .NET Framework 4.8 示例

本示例用于验证 `NewEmoji` C# 封装库在 WinForms `.NET Framework 4.8` 项目中的使用方式。

```powershell
dotnet run --project examples\CsharpNet48\MinimalWinFormsExample.csproj
```

示例会创建一个 new_emoji 原生窗口，展示中文文本、输入框、按钮和 emoji。点击“确认操作”后会触发 C# 事件并显示消息提示。
